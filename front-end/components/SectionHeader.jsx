const SectionHeader = ({ title }) => {
    return (
        <h2 className="pb-4 mb-4 text-custom-blue border-b border-custom-blue font-bold text-3xl">
            {title}
        </h2>
    );
};

export default SectionHeader;
